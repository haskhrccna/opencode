import 'package:flutter/material.dart';

/// Stub PendingStudentsScreen
class PendingStudentsScreen extends StatelessWidget {
  /// Constructor
  const PendingStudentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pending Students'),
      ),
      body: const Center(
        child: Text('Pending Students - To be implemented'),
      ),
    );
  }
}
